design.v

module and_gate ( input a,  input b,  output y  ); 
 assign y = a & b; 
 endmodule

tb.v

module and_gate_tb;
reg a;
reg b;
wire y;
and_gate dut (
    .a(a),
    .b(b),
    .y(y)
);
initial begin
    $dumpfile("tb.vcd");
    $dumpvars(0,and_gate_tb);
    $display ("A B| Y");
    $display("---------------");
    a=0;b=0;
    #10;
    $display("%b %b | %b",a,b,y);
    a=0;b=1;
    #10;
    $display("%b %b | %b",a,b,y);
    a=1;b=1;
    #10;
    $display("%b %b | %b",a,b,y);
    a=1;b=0;
    #10;
    $display("%b %b | %b",a,b,y);
    $finish;
end
endmodule
